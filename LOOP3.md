# Loop 3 — density, motion and accessibility

Reviewed the complete 80-image final section/state matrix and eight additional ask-focus/money-jump captures at 360×780, 390×844, 430×932 and 1280×900. Every capture was inspected through contact sheets, with full-size baseline comparisons. No page overflow in the final matrix.

Right: bronze controls, clear focus outlines, 44px controls and source/speaker targets, restrained motion with an explicit reduced-motion override. Section shortcuts put both the debate and available money pairing within reach from the top. The one-line taxonomy description sits beneath each name/count/spark row. The coverage rule is now a caption; every decade is fully visible. Serif reading text stays unboxed, with horizontal hairlines and mobile years above each item.

The two worst first-pass findings were fixed: pale bronze did not give small white button text enough contrast, and shrinking sparks through max-height flattened their relative values. Controls now use bronze-ink; spark heights are calculated directly in the smaller scale. A supplementary focus capture also exposed the old Promise.all early-rejection path: report-index failure could prematurely present counts as unavailable. Optional reads now fail independently.

Remaining limitations: many live results have no stored brief and retain a long passage. Some historical titles contain only speaker/date; the UI preserves the source instead of inventing debate names. Long taxonomy descriptions ellipsise visually on phones while retaining full accessible text/title. The desktop column leaves substantial right-hand paper, intentionally preserving reading width. The initial 30 are still a long scroll; the top shortcuts avoid making that scroll mandatory for money.

Validation: node --check portal/public/app.js; git diff --check. Behavioral probes passed initial 24+6 reads, next-slice-only reads, oldest ordering, final exhaustion, cached reuse, rapid toggle during a pending read, and 503 brief fallback. All 21 index links exceed 44px (minimum measured 78.09px). All measured topic controls, source/speaker links and bar links meet 44×44 at all four widths. Both section jumps transfer focus to the heading. AST comparison confirms only allowed topic functions and the directly adjacent description helper changed; the original CSS is an exact prefix of the final CSS. No Worker changes.
