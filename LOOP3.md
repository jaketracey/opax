# Loop 3 — navigation, touch and motion

Kept the ruler as a visual date overview and removed its overlapping invisible speech links; the full-size source rows provide the navigation. Restricted desktop source measure and made the options popover scroll within short viewports. Added visual-viewport compensation to the phone composer. Waiting illustrations and streaming painters are disposed when a chat request finishes.

Reviewed all 95 final captures: 24 at each phone width and 23 at 1280. They cover citation destination/return, every answer paragraph, both source disclosures and list ends, both options scroll positions, suggestions, chat sources, waiting, completed follow-up, reduced motion, and a 480px-tall phone viewport. The two worst findings were an empty waiting stage during the loader's entrance and no direct return from a source to its citation. Both were fixed and recaptured: start the loader on stage; provide a 44px “Back to answer” control restoring focus to the exact marker.

Functional checks passed for all 12 citation round trips, seeded and reloaded chat citations, the complete 11-source cited list, malformed/missing citation ranges, shared answer rendering without evidence, and browser exceptions (none). Older payloads without a cited flag retain the citation-derived split when passed into chat. All four layouts report no horizontal document overflow. Phone composer bottoms equal the viewport heights; all three follow-ups remain visible together at 480px height. Follow-up animation is disabled under reduced motion.

Final critique: the real answer still has long paragraphs, and seven references at the end of its first paragraph occupy a second line on small phones. That reflects the response's paragraph-level evidence, not sentence-level evidence we can invent. Some source titles remain generic and parties missing; actual passages and metadata are preserved. Source units are intentionally taller to accommodate touch targets. The desktop footer and broader site chrome remain outside this page's scope. A physical keyboard/browser-chrome test remains necessary.

Verification: node --check portal/public/app.js; git diff --check; local browser interaction harness. Worker untouched, so no Worker TypeScript check required. Existing stylesheet prefix was checked byte-for-byte against the starting commit: all CSS is appended beneath the single requested banner.
