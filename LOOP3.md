# Loop 3 — typography, accessibility and resilience

The summary keeps its single bronze-on-paper treatment and the exact “not part of the record” note, now at 14px. Its heading and the record heading use the house serif; the machine-summary label is plain sans text rather than a capsule. Numeric content is tabular. Close controls at the end of citations and similar results restore focus to the trigger, and Escape closes the focused disclosure. Focus rings are explicit. The page adds no motion and disables inherited element transitions/animations under reduced motion.

Reviewed 99 final captures: 84 from the three real documents across 360×780, 390×844, 430×932 and 1280×900, plus 15 interaction/fixture captures. These include citations and similar results at both ends, summaries, source reading, caveat, keyboard focus, loading/error/empty/missing-brief states, a clearly synthetic chair-call example at all four widths, and 200% text. Every capture was inspected, using contact sheets and full-size views for detail. Revised captures replace preliminary images under the same loop filenames.

The two worst final findings were addressed before close: five fixed toolbar columns became cramped at 200% text, and same-debate links repeated the speaker in their metadata. The toolbar now wraps further when text grows, retaining two rows at normal phone sizes. A font-relative container rule lets enlarged header metadata use the full width. Duplicate names are removed from related metadata. The large-text recheck confirms no page overflow or internal button clipping; the focused Cite control has a visible bronze outline.

Final critique: at 360px the committee's source title remains long, and its exact bracketed context is repeated below the summary. That is verbose, but it is readable and preserves the record. NSW remains a continuous paragraph because its source supplies no line breaks. The 11,578-word federal sample remains a long scroll, with source concatenation artefacts and footer residue unchanged. Similar results can be tangential even with a known topic filter; they are discovery links, not evidence of adjacency. Desktop retains the deliberately narrow reading column and substantial right-hand paper. The citation monospace is compact; test selection on a physical phone.

Checks passed: JavaScript syntax, whitespace, source-preservation regression probe, disclosure close/focus restoration, Escape, error/retry, empty retrieval, missing-brief fallback, stale response after navigating to another document, reduced-motion captures, and 200% type. The interaction probe recorded zero page errors and zero ask requests. All 12 real-document viewport combinations have zero horizontal overflow and zero document targets below 44px. CSS/HTML/JS boundary comparisons against the starting commit passed. No Worker check was needed because the Worker was not touched.
