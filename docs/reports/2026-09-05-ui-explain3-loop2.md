# Loop 2 critique — history hierarchy, close clearance and citation lanes

Reviewed all five steps from the Westpac donor and Liberal party entries at 360×780, 390×844 and 430×932: 30 WebGL captures through ANGLE/SwiftShader. Every record request was intercepted with six citations from the same year; no paid ask was made.

1. **Who gives**
   - Loop 1’s shorter plate, five-label ladder and edge-safe camera remain intact. The larger 11px phone labels still clear their rings and are more credible than the original miniature ledger.
   - The 360px Westpac capture initially found a 1.55px overlap between the two stage headings after the close buffer increased. A narrow-only width cap now separates them without introducing a third line.
2. **How much, and when**
   - The histogram is finally readable rather than merely present. Phone bars are 58% taller, rendered as filled slivers instead of one-pixel WebGL line segments, and retain enough weight for non-peak years to form a deliberate skyline.
   - Election labels and the peak annotation are now 11px. All ten election labels remain separate at 360px, although the baseline is necessarily compact.
   - When the seven-second run ends, the large counter resolves to the peak year and amount (`2014 · $21.4M` for Westpac; `2013 · $31.8M` for Liberal), not the tiny or empty terminal year. The stage-level peak annotation is technically redundant at that point, but the repetition makes the peak unambiguous and avoids giving 2025 false authority.
3. **Where it lands**
   - The lower-edge gauge caption and five-label ladder survive the stronger history typography without a shift or collision.
   - The smallest ribbons remain close together near the receiver, but their filled width and separate ring landings keep them traceable.
4. **What parliament said**
   - The six-source same-year stress case now uses 11px dates, taller lane spacing and card plates wide enough to sit behind the complete date. Party-coloured card edges are visible rather than reduced to a tiny underline.
   - The first capture still had up to 0.8px of adjacent date overlap at 360px and cards narrower than their text. Both were corrected before the final matrix. Final computed overlap is zero at all widths; all six cards fit without clustering and remain individually highlightable from the source list.
   - The phone stage still carries dates rather than speech titles. That remains the right information limit: titles live in the narrative where they can wrap without consuming the scene.
5. **What this cannot prove**
   - The disclosure-floor composition remains stable, but its two labels are now the only scene text below 11px. Loop 3 must fix them and pull the state/federal note closer to the crossed arc.

Final diagnostics found zero scene-label overlaps and zero horizontal dialog overflow. The top-right label keeps a 14.4px measured gap from the close control on every step at 360, 390 and 430px. The two worst loop-2 findings—the 360px stage-heading contact and the same-year citation-lane contact—were fixed before close.
